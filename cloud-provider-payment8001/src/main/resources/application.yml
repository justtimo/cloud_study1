server:
  port: 8001

spring:
  application:
    name: cloud-provider-payment8001
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cloud_study1?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: wby6225104

mybatis:
  mapperLocations: classpath:/mapper/*.xml
  type-aliases-package: com.wby.springcloud.bean

eureka:
  client:
    register-with-eureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://localhost:7001/eureka

